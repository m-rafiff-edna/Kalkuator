name: kalkulator_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you want to publish your package

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  math_expressions: ^2.0.0  # Add this line
  # Other dependencies can go here

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
